import { Observable } from 'rxjs';
export declare class UploadFile {
    readonly file: File;
    private _progress;
    icon: string;
    private imageSource;
    constructor(file: File, _progress?: number);
    progress: number;
    supportsImagePreview(): boolean;
    getImageSource(): Observable<string>;
    increaseProgress(add: number): void;
}
